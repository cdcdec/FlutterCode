# Dart集合

1. List集合

	```

   // Use a List constructor. 使用List的构造函数
   var vegetables = new List();
   
   // Or simply use a list literal.  使用列表
   var fruits = ['apples', 'oranges'];
   
   //list是泛型类型也可以指定里面保存的类型
   var fruits = new List<String>();
   fruits.add('apples');
   ```



